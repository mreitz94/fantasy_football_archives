<nav class="flex items-center justify-between flex-wrap bg-blue-600 p-3">
  <div class="flex items-center flex-shrink-0 text-white mr-6">
    <span class="font-semibold tracking-tight"><%= @league.name %></span>
  </div>

  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
    <div class="text-sm lg:flex-grow">
      <%= link_to 'Standings', standings_league_path(@league), class: 'block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-100 mr-4' %>
      <%= link_to 'Teams', league_teams_path(@league), class: 'block mt-4 lg:inline-block lg:mt-0 text-white hover:text-gray-100 mr-4' %>
    </div>
  </div>
</nav>
